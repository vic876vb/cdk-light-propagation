export type Configuration = {
  fillColor?: string,
  strokeColor?: string,
  thickness?: number,
  frontRadius?: number,
  backRadius?: number,
}

export async function postConfiguration(configuration: Configuration) {
}